<% layout("/layouts/boilerplate") %>
<body>
    <div class="row mt-3">
        <div class="col-8 offset-0">
            
    <h3><%= listing.title%></h3>
    </div>
    <div class="card col-6 show-card">
        <img src="<%= listing.image.url %>" class="card-img-top show-img" alt="listing_image">
  <div class="card-body">
    
    
    <br>
     <%= listing.description%>
     <br>
      &#8377;<%= listing.price?.toLocaleString("en-IN")%>
      <br>  
      <%= listing.location%>
      <br>
      <%= listing.country%>
    </p>

  </div>
    </div>
    
    <br/>
    <div class="btns">
<a href="/listings/<%= listing._id %>/edit" class="btn edit-btn btn-dark col-1 offset-0">Edit</a>
    <br></b>
    <form action="/listings/<%= listing._id %>?_method=DELETE" method="POST">
    <button type="submit" class="btn btn-dark mb-3  offset-4">Delete</button>
    </div>
    <hr>
</form>
    </div>
    <div class="col-8 offset-0 mb-3">
        <h4>Leave a Review</h4>
        <form action=""  >
            <div>
                <label for="rating" class="form-label form-range">Rating</label>
                <input type="range" min="1" max="5" id="rating" name="review[rating]">

            </div>
            <div>
                <label for="comment" class="form-label" >Comments</label>
                <textarea name="review[comment]" id="comment" cols="30" class="form-control" rows="10"></textarea>
            </div>
            <button class="btn btn-outline-dark mt-3">Submit</button>
        </form>
    </div>
</body>

        
